Hi <%= @user.name %>,

It's just a notification that you have sent a request to join trip "<%= @trip.track.name %>".
Full trip details are available by url: <%= trip_url(@trip.id) %>

Thank you for using our service,
Regards, pohody.com.ua