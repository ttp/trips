<%= @trip.track.name %> (<%= t(@trip.track.region.name, :scope => "region") %>)

<%= t('dates') %>: <%= l(@trip.start_date) + '-' + l(@trip.end_date) %>

<%= t('trip_join_mailer.full_details_url') %>: <%= trip_url(@trip.id) %>

