!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}
  %body
    %p
      It's just a notification that you have sent a join request for the next trip:
    %p
      %b= @track.name
      (#{t(@track.region.name, :scope => 'region')})
      %br
      Dates: #{l(@trip.start_date) + " - " + l(@trip.end_date)}
    %p
      Full trip details are available on the trip page
      %a{href: trip_url(@trip.id)}= trip_url(@trip.id)

    %p
      Thank you for using our service,
      %br
      %a{href: root_url} pohody.com.ua