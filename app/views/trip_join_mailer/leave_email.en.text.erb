User "<%= @user.name %>" has left trip:
<%= @trip.track.name %> (<%= t(@trip.track.region.name, :scope => "region") %>)
Dates: <%= l(@trip.start_date) + '-' + l(@trip.end_date) %>

Full trip details are available by url: <%= trip_url(@trip.id) %>

Thank you for using our service,
pohody.com.ua